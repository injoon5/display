# Fetcher writes source payloads into Convex via the client API URL.
CONVEX_URL=http://127.0.0.1:3210
# Optional service auth for non-anonymous Convex deployments
# CONVEX_SERVICE_TOKEN=
# CONVEX_DEPLOY_KEY=
# Per-plugin HTTP timeout
FETCH_TIMEOUT_MS=15000
